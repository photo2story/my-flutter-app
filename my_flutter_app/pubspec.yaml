name: my_flutter_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  discord_logger: ^1.0.0 # 최신 버전으로 업데이트

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

