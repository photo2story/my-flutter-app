name: my_flutter_app
description: A new Flutter project.

# 버전 번호는 프로젝트에 맞게 수정하세요
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # http 패키지 추가 (최신 버전으로 업데이트)
  http: ^1.2.1

  # photo_view 패키지 추가
  photo_view: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
